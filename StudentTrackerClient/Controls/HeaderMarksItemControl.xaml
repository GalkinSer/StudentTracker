<UserControl x:Class="StudentTrackerClient.Controls.HeaderMarksItemControl"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:StudentTrackerClient.Controls" xmlns:viewmodels="clr-namespace:StudentTrackerClient.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:HeaderMarksItemControlViewModel}"
			 mc:Ignorable="d" 
			 d:DesignHeight="800" d:DesignWidth="200">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="22"></RowDefinition>
			<RowDefinition Height="*"></RowDefinition>
		</Grid.RowDefinitions>
		<TextBox x:Name="TitleTextBox" Grid.Row="0" Text="{Binding Header.Title}">
            <TextBox.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Удалить" Command="{Binding DeleteHeaderCommand}"></MenuItem>
                </ContextMenu>
            </TextBox.ContextMenu>
        </TextBox>
		<ItemsControl x:Name="MarksItemsControl" Grid.Row="1" ItemsSource="{Binding Header.Marks}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<local:MarkItemControl></local:MarkItemControl>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</Grid>
</UserControl>
