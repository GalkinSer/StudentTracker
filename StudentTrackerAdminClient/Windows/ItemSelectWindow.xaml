<Window x:Class="StudentTrackerAdminClient.Windows.ItemSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentTrackerAdminClient.Windows" xmlns:viewmodels="clr-namespace:StudentTrackerAdminClient.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:ItemSelectWindowViewModel}"
        mc:Ignorable="d"
        Title="ItemSelectWindow" Height="150" Width="200">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
        </Grid.RowDefinitions>
        <ComboBox Name="MainComboBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4"
                  ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"></ComboBox>

        <Button x:Name="OkButton" Content="OK" Grid.Row="3" Grid.Column="4"
            Command="{Binding Ok}" IsEnabled="{Binding IsOkEnabled}"></Button>
        <Button x:Name="CancelButton" Content="Отмена" Grid.Row="3" Grid.Column="2"
            Command="{Binding Cancel}"></Button>

    </Grid>
</Window>
