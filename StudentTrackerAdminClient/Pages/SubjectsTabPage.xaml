<Page x:Class="StudentTrackerAdminClient.Pages.SubjectsTabPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentTrackerAdminClient.Pages" xmlns:viewmodels="clr-namespace:StudentTrackerAdminClient.ViewModels" xmlns:tabpagesviewmodels="clr-namespace:StudentTrackerAdminClient.ViewModels.TabPagesViewModels" d:DataContext="{d:DesignInstance Type=tabpagesviewmodels:SubjectsTabPageViewModel}"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SubjectsTabPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="0.75*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="150"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="35"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
        </Grid.RowDefinitions>
        <ToolBar x:Name="MainToolBar" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="5">
            <Separator Width="5" Background="Transparent"></Separator>
            <Button x:Name="AddItemButton" Width="25" Command="{Binding AddItem}">
                <Image Source="/Resources/add.png"></Image>
            </Button>
            <Separator Width="5" Background="Transparent"></Separator>
            <Button x:Name="RemoveItemButton" Width="25" Command="{Binding RemoveItem}">
                <Image Source="/Resources/delete.png"></Image>
            </Button>
            <Separator Width="5" Background="Transparent"></Separator>
            <Button x:Name="SaveItemButton" Width="25" Command="{Binding SaveItem}">
                <Image Source="/Resources/save.png"></Image>
            </Button>
        </ToolBar>
        <ListBox x:Name="MainListBox" Grid.Row="3" Grid.Column="1" Grid.RowSpan="7"
         ItemsSource="{Binding Subjects}" SelectedItem="{Binding SelectedSubject}"></ListBox>
        <TextBlock x:Name="SubjectNameTextBlock" Grid.Row="3" Grid.Column="3"
           Text="Название: " FontSize="25" FontWeight="Bold"></TextBlock>

        <TextBox x:Name="SubjectNameTextBox" Grid.Row="3" Grid.Column="5"
           Text="{Binding SelectedSubject.Name}" FontSize="20"></TextBox>
    </Grid>
</Page>
